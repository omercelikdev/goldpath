# rulesets/

Empty **by design**: Spec Engine rulesets ship inside generated apps (`.specdrift/rules.yaml`
+ `.specdrift/drift.yaml`, born from the template) — the rules live where the manifest they
police lives. Engine and rule ids: `docs/rfc/spec-engine-v1.md`.
