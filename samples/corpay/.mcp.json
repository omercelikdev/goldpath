{
  "mcpServers": {
    "specdrift": {
      "command": "specdrift",
      "args": ["mcp"],
      "comment": "The deterministic spec engine (dotnet tool install -g specdrift). Skills MUST consult spec_validate/spec_drift before declaring work done - see .claude/skills/."
    }
  }
}
